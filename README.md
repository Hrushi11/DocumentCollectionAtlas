# The Document Collection Problem

The complete design lives in **[`docs/`](docs/README.md)** — read that first. It covers the
problem analysis, data/classifier decisions (backed by real IRS-form extraction), the schema,
the core algorithms (derivation + reconciliation + matching), the end-to-end test scenarios,
and a milestone build plan.

A proper "how to run it / decisions / what I left out / what's next" README will replace this
file once the build starts.
