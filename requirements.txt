# Runtime
Flask==3.1.3
SQLAlchemy==2.0.52
pypdf==6.16.1          # AcroForm form-field extraction (TieredExtractor tier 1) + fixture fill
pdfplumber==0.11.10   # text-layer extraction + word coordinates (tier 2)

# Build-time only (fixture generation / rasterizing the unreadable scan) — not a runtime dep
pypdfium2==5.13.0

# Tests
pytest==9.1.1
